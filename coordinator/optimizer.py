# coordinator/optimizer.py

def optimize(sql: str) -> str:
    """
    Placeholder optimizer.
    Will later support simple rewrites, projection push-down, etc.
    For now, returns the SQL unchanged.
    """
    return sql
